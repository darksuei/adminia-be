const Router = require("express").Router;

const router = Router();

router.get("/user/:id", () => {});

router.post("/user", () => {});

router.put("/user", () => {});

router.delete("/user", () => {});

router.post("/new_user", () => {});

module.exports = router;
